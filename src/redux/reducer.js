import { SIGN_UP_FAILURE, SIGN_UP_SUCCESS } from "./action-types";

let initialState = {};

const Reducer = (state = initialState, action) => {
    switch(action.type){
        default: return state;

    }
}
export default Reducer;